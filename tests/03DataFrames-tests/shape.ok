test = {
    'name': 'shape',
    'suites': [
        {
            'cases': [
                {
                    'code': r"""
                    >>> # It looks like the data_shape variable hasn't been defined.
                    >>> # Did you remember to run the cell?
                    >>> data_shape in vars()
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # data_shape should be a tuple: two numbers separated by a comma in parentheses.
                    >>> # Make sure you are getting the shape using the "shape" attribute on the "ads" DataFrame.
                    >>> type(data_shape) == tuple
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # 
                    >>> data_shape[0] == 588101
                    True
                    """
                }
            ]
        }
    ]
}