test = {
    'name': 'ratings',
    'suites': [
        {
            'cases': [
                {
                    'code': r"""
                    >>> # It looks like the rating_counts variable hasn't been defined.
                    >>> # Did you remember to run the cell?
                    >>> rating_counts in vars()
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # Your answer doesn't have the right format yet-
                    >>> # it should be a DataFrame, meaning it looks like a table.
                    >>> type(rating_counts) == pandas.core.frame.DataFrame
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # You may have grouped by the wrong variable.
                    >>> # For this question, we want to form groups by handbag ratings.
                    >>> # You should see "rating" as the name of the first column in bold.
                    >>> rating_counts.index.name == "rating"
                    True
                    """
                }
            ]
        }
    ]
}