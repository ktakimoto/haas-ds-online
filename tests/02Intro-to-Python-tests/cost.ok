test = {
    'name': 'cost',
    'suites': [
        {
            'cases': [
                {
                    'code': r"""
                    >>> # It looks like the cost variable hasn't been given a value.
                    >>> # Did you remember to run the cell where the cost variable was defined?
                    >>> 'cost' in vars()
                    >>>
                    True
                    """
                },                
                {
                    'code': r"""
                    >>> # Remember, CPM is the cost per THOUSAND ads.
                    >>> # What do you need to do to get the cost per ad?
                    >>> cost_per_ad == cpm / 1000
                    True
                    """
                },                
            ]
        }
    ]
}