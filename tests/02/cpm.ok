test = {
    'name': 'cpm',
    'suites': [
        {
            'cases': [
                {
                    'code': r"""
                    >>> # It looks like total_ads hasn't been defined yet.
                    >>> # Did you remember to add your answer and run the cell?
                    >>> ('total_ads' in vars()) and (total_ads != ...)
                    True
                    """
                },                
                {
                    'code': r"""
                    >>> # Double-check that you've entered the correct number of ads
                    >>> 14000000 < total_ads < 15000000
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # It looks like cpm hasn't been defined yet.
                    >>> # Did you remember to add your answer and run the cell?
                    >>> ('cpm' in vars()) and (cpm != ...)
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # Double-check your value of CPM.
                    >>> # Check the yellow exercise cell for the values.
                    >>> 8.5 < cpm < 9.5
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # It looks like cost_per_ad hasn't been defined yet.
                    >>> # Did you remember to add your answer and run the cell?
                    >>> ('cost_per_ad' in vars()) and (cost_per_ad != ...)
                    True
                    """
                },
                {
                    'code': r"""
                    >>> # Remember, CPM is the cost per THOUSAND ads.
                    >>> # What do you need to do to get the cost per ad?
                    >>> 0 < cost_per_ad < 0.01
                    True
                    """
                }               
            ]
        }
    ]
}